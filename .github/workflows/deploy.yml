name: Deploy Billing App to OCI

on:
  push:
    branches: [ "development" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/billing-app-ui/package-lock.json

      - name: Install UI Dependencies
        run: npm ci
        working-directory: src/billing-app-ui

      - name: Build UI
        run: npm run build -- --configuration production
        working-directory: src/billing-app-ui

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish API
        run: dotnet publish BillingApp.API/BillingApp.API.csproj -c Release -o publish
        working-directory: src

      - name: Prepare Deployment Bundle
        run: |
          mkdir -p deploy/src/billing-app-ui/dist/billing-app-ui
          mkdir -p deploy/src/BillingApp.API
          cp docker-compose.yml deploy/
          cp .env.example deploy/
          cp src/billing-app-ui/Dockerfile deploy/src/billing-app-ui/
          cp src/billing-app-ui/nginx.conf deploy/src/billing-app-ui/
          cp -r src/billing-app-ui/dist/billing-app-ui/browser deploy/src/billing-app-ui/dist/billing-app-ui/
          cp src/BillingApp.API/Dockerfile deploy/src/BillingApp.API/
          cp -r src/publish deploy/src/
          tar -czf billing-app.tar.gz -C deploy .

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          # Using a heredoc with quotes to prevent shell execution/expansion
          cat <<'EOF' > ~/.ssh/id_rsa
          ${{ secrets.OCI_KEY }}
          EOF
          chmod 600 ~/.ssh/id_rsa
          # Ensure key has correct line endings and a trailing newline
          sed -i 's/\r$//' ~/.ssh/id_rsa
          sed -i '$a\' ~/.ssh/id_rsa
          # Basic validation: check if it looks like a private key
          if ! grep -q "BEGIN" ~/.ssh/id_rsa; then
            echo "ERROR: OCI_KEY secret does not appear to be a valid private key."
            exit 1
          fi

      - name: Copy Bundle to Server (Raw SCP)
        run: |
          scp -vvv -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=120 -o ConnectionAttempts=10 -o IPQoS=0x00 -o ServerAliveInterval=15 -o PreferredAuthentications=publickey -i ~/.ssh/id_rsa billing-app.tar.gz ${{ secrets.OCI_USERNAME }}@${{ secrets.OCI_HOST }}:/home/ubuntu/

      - name: Deploy with Docker Compose (Raw SSH)
        run: |
          ssh -vvv -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=120 -o ConnectionAttempts=10 -o IPQoS=0x00 -o ServerAliveInterval=15 -o PreferredAuthentications=publickey -i ~/.ssh/id_rsa ${{ secrets.OCI_USERNAME }}@${{ secrets.OCI_HOST }} << 'EOF'
            set -e
            # Create app directory if not exists
            mkdir -p /home/ubuntu/billing-app
            
            # Extract the bundle
            tar -xzf /home/ubuntu/billing-app.tar.gz -C /home/ubuntu/billing-app
            rm /home/ubuntu/billing-app.tar.gz

            cd /home/ubuntu/billing-app

            # Fix Oracle Cloud Ubuntu Firewall (iptables)
            sudo iptables -I INPUT -p tcp --dport 80 -j ACCEPT || true
            sudo iptables -I INPUT -p tcp --dport 443 -j ACCEPT || true
            sudo iptables -I INPUT -p tcp --dport 81 -j ACCEPT || true
            sudo netfilter-persistent save || echo "rules not saved"

            # Create .env if missing
            if [ ! -f .env ]; then
                cp .env.example .env
            fi

            # Deploy with Docker Compose
            sudo docker compose up -d --build
            
            # Clean up old images to save space
            sudo docker image prune -f

            # Diagnostics
            echo "--- Container Status ---"
            sudo docker ps
          EOF
